/**
 * @module L6.Assembly.Guidance
 * phase 9: composer for inbox type `contract_audit_feedback` — NO_GUIDANCE sentinel.
 *
 * body 已含 auditor 列 drift 项 + next_focus_suggestion（contract-auditor.deliverFeedback 拼）/ LLM 自决.
 */

import { NO_GUIDANCE } from '../types.js';

export const composer = NO_GUIDANCE;
