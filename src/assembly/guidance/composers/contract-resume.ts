/**
 * @module L6.Assembly.Guidance
 * phase 9: composer for inbox type `contract_resume` — NO_GUIDANCE sentinel.
 *
 * body 已含 contract id + pending subtasks 列表（sender 拼）/ LLM 自决.
 */

import { NO_GUIDANCE } from '../types.js';

export const composer = NO_GUIDANCE;
